Функция проверяет, имеет ли массив от 1 до 3 префиксов 'h', 'm', 's' (часы, минуты и секунды) и, если они есть, извлекает значения времени, связанные с этими префиксами. Затем она проверяет порядок префиксов, чтобы убедиться, что он корректен (например, 'hms' или 'hm', но не 'hsm'). Далее она проверяет значения времени, чтобы убедиться, что они находятся в допустимом диапазоне, а именно, что часы находятся в диапазоне от 0 до 23, а минуты и секунды находятся в диапазоне от 0 до 59. Если в процессе проверки возникнет ошибка, функция вернет false. Если все проверки успешны, функция вернет true и выведет сообщение "All validation is ok. Code - 201".
